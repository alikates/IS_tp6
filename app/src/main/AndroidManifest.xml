<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.unizar.eina.appPedidos">

    <uses-permission android:name="android.permission.SEND_SMS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppPedidos">
        <activity
            android:name="es.unizar.eina.appPedidos.ui.PedidoEdit"
            android:exported="false"/>
        <activity
            android:name="es.unizar.eina.appPedidos.ui.ProductoEdit"
            android:exported="false"/>
        <activity
            android:name="es.unizar.eina.appPedidos.ui.AppPedidos"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>